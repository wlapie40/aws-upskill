##todo deploy.sh
1. export FLASK_ENV= dev or docker  or prod
2. python deploy.py

2. *pipenv shell
3. python database_conf_gen.py
5. docker-compose down
6. docker-compose up -d / docker-compose up --build
7. *pipenv run python app.py / python app.py (virtualenv)
